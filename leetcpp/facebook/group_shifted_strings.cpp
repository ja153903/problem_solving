#include <string>
#include <vector>

// TODO: Do this problem later at night:
class Solution {
public:
  std::vector<std::vector<std::string>>
  groupStrings(std::vector<std::string> &strings) {
  }
};
